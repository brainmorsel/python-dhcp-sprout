{% extends "base_layout.jinja2" %}
{% block title %}Assigned List{% endblock %}
{% block content %}
    <h1>{{ self.title() }}</h1>
    <table class="table sortable">
        <thead>
            <tr>
                <th>Действие</th>
                <th>ID</th>
                <th>Профиль</th>
                <th>Relay IP</th>
                <th>IP</th>
                <th>MAC</th>
                <th>Дата первого запроса</th>
                <th>Дата последнего запроса</th>
            </tr>
        </thead>
        <tbody>
        {% for i in items %}
        <tr>
            <td>
                <a href="{{ i.id }}/delete"
                   onclick="return confirm('Точно удалить {{ i.mac_addr }}?');">Удалить</a>
            </td>
            <td>{{ i.id }}</td>
            <td>{{ i.profile_name }}</td>
            <td>{{ i.relay_ip }}</td>
            <td>{{ i.ip_addr }}</td>
            <td>{{ i.mac_addr }}</td>
            <td>{{ i.create_date }}</td>
            <td>{{ i.lease_date }}</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
